<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sanavi.aiapi.analysis.mapper.AnalysisResultMapper">

    <insert id="insertAnalysisResult" parameterType="com.sanavi.aiapi.analysis.dto.AnalysisResultDto">
        INSERT INTO analysis_result (id, user_id, disease, inspector, job, base_score, created_at, deleted)
        VALUES (#{id}, #{userId}, #{disease}, #{inspector}, #{job}, #{baseScore}, #{createdAt}, #{deleted})
    </insert>

    <update id="updateBaseScore">
        UPDATE analysis_result SET base_score = #{baseScore} WHERE id = #{id}
    </update>

    <select id="existsChatByResultId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM analysis_result_chat WHERE result_id = #{resultId}
    </select>

    <insert id="insertChat">
        INSERT INTO analysis_result_chat (result_id, sender_type, chat_content)
        VALUES (#{resultId}, 'AI', #{chat_content})
    </insert>

    <insert id="insertChecklist">
        INSERT INTO analysis_result_check (result_id, label, purpose, method, reason)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{resultId}, #{item.label}, #{item.purpose}, #{item.method}, #{item.reason})
        </foreach>
    </insert>

    <insert id="insertWarnings">
        INSERT INTO analysis_result_warning (result_id, warning)
        VALUES
        <foreach collection="warnings" item="warning" separator=",">
            (#{resultId}, #{warning})
        </foreach>
    </insert>

    <insert id="insertMetaContents">
        INSERT INTO analysis_result_metacontent (result_id, meta_content)
        VALUES
        <foreach collection="metaContents" item="metaContent" separator=",">
            (#{resultId}, #{metaContent})
        </foreach>
    </insert>

    <!-- Input:  userId / Output: AnalysisHistoryItemDto 목록 / 책임: 유저별 분석 이력 최신순 조회 (deleted=1만) -->
    <select id="findHistoryByUserId" resultType="com.sanavi.aiapi.analysis.dto.AnalysisHistoryItemDto">
        SELECT id, disease, job, base_score, created_at
        FROM analysis_result
        WHERE user_id = #{userId} AND deleted = 1
        ORDER BY created_at DESC
    </select>

    <!-- Input:  id, userId / Output: 변경 행 수 / 책임: 논리 삭제 (deleted=0) — userId 불일치 시 0 반환 -->
    <update id="softDeleteById">
        UPDATE analysis_result
        SET deleted = 0
        WHERE id = #{id} AND user_id = #{userId} AND deleted = 1
    </update>

</mapper>
