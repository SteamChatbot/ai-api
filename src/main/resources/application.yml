spring:
  datasource:
    url:
    username:
    password:
    driver-class-name: org.mariadb.jdbc.Driver

  data:
    redis:
      host:
      port: 6379
      password:

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.sanavi.backend
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret:
  access-expiration:
  refresh-expiration:

server:
  port: 8080
